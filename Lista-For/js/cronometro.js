for (let horas = 00; horas < 25 ; horas++) {
    for (let minutos = 00 ; minutos < 60; minutos++) {
        for (let segundos = 00; segundos < 60; segundos++) {
            document.write(horas+" : "+minutos+" . "+segundos+"</br>");
        }
    }
}